source "https://rubygems.org"

# 這是 Jekyll 的核心
gem "jekyll"

# 這是你用的主題
gem "minima"

# 將外掛放在這個分組內，GitHub Actions 才能正確識別並啟用它們
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
  gem "jekyll-sitemap"
end
